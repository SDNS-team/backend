{
  "root": "apps/user",
  "sourceRoot": "apps/user/src",
  "projectType": "application",
  "targets": {
    "deploy": {
      "builder": "@nrwl/workspace:run-commands",
      "options": {
        "commands": [
          "nx build user",
          "npx prisma generate --schema=./apps/user/prisma/schema.prisma",
          "docker build --platform linux/amd64 --no-cache --progress=plain -t birthdayapp_development_user -f ./apps/user/Dockerfile ."
        ],
        "parallel": false
      }
    },
    "build": {
      "executor": "@nrwl/node:webpack",
      "outputs": ["{options.outputPath}"],
      "options": {
        "outputPath": "dist/apps/user",
        "main": "apps/user/src/main.ts",
        "tsConfig": "apps/user/tsconfig.app.json"
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false,
          "fileReplacements": [
            {
              "replace": "apps/user/src/environments/environment.ts",
              "with": "apps/user/src/environments/environment.prod.ts"
            }
          ]
        }
      }
    },
    "serve": {
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "user:build"
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/user/**/*.ts"],
        "fix": true
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["coverage/apps/user"],
      "options": {
        "jestConfig": "apps/user/jest.config.ts",
        "passWithNoTests": true
      }
    }
  },
  "tags": []
}
